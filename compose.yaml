services:
  api:
    build:
      context: .
    env_file:
      - path: .env
        required: false
    ports:
      - "8000:8000"
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /tmp
    restart: unless-stopped
